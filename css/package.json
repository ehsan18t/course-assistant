{
  "name": "TailWind",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:tailwind": "tailwind build src/styles.css -o styles.css --watch",
    "build:modal": "tailwind build src/modal.css -o modal.css --watch",
    "build:stats": "tailwind build src/stats.css -o stats.css --watch",
    "build:chat": "tailwind build src/chat.css -o chat.css --watch",
    "build:post": "tailwind build src/post.css -o post.css --watch",
    "build:profile": "tailwind build src/profile.css -o profile.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.5.1",
    "prettier-plugin-tailwindcss": "^0.1.5",
    "tailwindcss": "^3.0.15"
  },
  "dependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.5",
    "postcss-import": "^14.0.2",
    "tailwind": "^4.0.0"
  }
}
